name: mapr-slave
revision: 1
summary: Slave Node for MapR
description: slave node for mapr
provides:
  mapr-node:
    interface: mapr-node
  zk-node:
    interface: zk-node
  ganglia:
    interface: monitor
